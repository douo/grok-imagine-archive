services:
  grok-imagine-archive:
    build: .
    ports:
      - "127.0.0.1:7860:7860"
    environment:
      GROK_IMAGINE_ARCHIVE_ROOT: /data/archive
      GROK_IMAGINE_ARCHIVE_WEB_TOKEN: change-me
    volumes:
      - ./archive:/data/archive
      - ./config:/app/config:ro
